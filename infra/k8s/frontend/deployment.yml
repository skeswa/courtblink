apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 3
  template:
    metadata:
      labels:
        component: frontend
    spec:
      containers:
      - name: frontend
        image: us.gcr.io/courtblink/frontend:1.0.0